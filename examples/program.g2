fn loop(limit: int): void => 
{
    // Start count
    let count = 0;

    // While loop
    while (count < limit)
    {
        printf("Loop %d\n", count); // Print the current count

        // Increment count
        count = count + 1;
    }
}

fn main(): int => 
{
    loop(10);
    return 0;
}