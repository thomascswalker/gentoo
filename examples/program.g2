fn main(): int => 
{
    // Start count
    let count = 0;

    // While loop
    while (count < 100)
    {
        printf("Loop %d\n", count); // Print the current count

        // Increment count
        count = count + 1;
    }
    return 0;
}